${previewBlurb}

<div class="inset">
<p>When you require something less artificial... </p>
<p>...something that unleashes you own creativity, instead of some semi-random automatic glorified swag...</p>
<h2>keep it real with</h2>
<h1>Panopset Flywheel</h1>
<p>
A freeform template processor, with a simple shell script like syntax, it takes the tedium out of your work, freeing
up your creative energy.
</p>
<p>
With flywheel, you never have to write the same thing twice.
<ul><li>
There is a <a href="./flywheel.html">web version</a>, for quick tasks that wouldn't be so quick without it,
</li><li>
a script version, fw, which is <a href="https://github.com/panopset/src/blob/main/code/slab/templates/driver.txt">used</a>
to publish this site, and the
</li><li>
<a href="./downloads.html">desktop</a> version, flywheel, which has additional capabilities and features just a tab away.
</li></ul>
</p>
</div>
